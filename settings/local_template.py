"""
Local settings template.
Copy this file to local.py and adjust settings for your development environment.

The local.py file is ignored by git and should never be committed.
It should contain only the settings that differ from the defaults.
"""

# Uncomment and adjust any settings you need to override

# Secret key for local development (Don't use in production!)
# SECRET_KEY = "different-local-secret-key"

# Debug settings
# DEBUG = True

# Database settings - only override if needed
# DATABASES = {
#     'default': {
#         'ENGINE': 'django.db.backends.postgresql',
#         'NAME': 'my_custom_db_name',
#         'USER': 'my_custom_username',
#         'PASSWORD': 'my_custom_password',
#         'HOST': 'localhost',
#         'PORT': '5432',
#     }
# }

# Cache settings
# CACHES = {
#     'default': {
#         'BACKEND': 'django.core.cache.backends.locmem.LocMemCache',
#         'LOCATION': 'unique-snowflake',
#     }
# }

# Static files
# STATIC_URL = '/static/'
# STATIC_ROOT = BASE_DIR / 'staticfiles'

# Custom app settings
# For example:
# MY_CUSTOM_SETTING = 'custom value'
